# .env.example

## Environment Variables Example
This file provides an example of the environment variables that should be set for the project.

### Required Variables
- **API_KEY**: Your API key for accessing external services.
- **DATABASE_URL**: The URL for connecting to the database.
- **PORT**: The port number on which the application will run.

### Optional Variables
- **DEBUG**: Set to `true` to enable debug mode.
- **LOG_LEVEL**: The logging level (e.g., `info`, `warn`, `error`).

## Usage
Copy this file to `.env` and update the values as needed for your environment.