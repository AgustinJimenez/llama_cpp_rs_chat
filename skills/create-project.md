---
name: create-project
description: Create a new project with standard structure and dependencies
---

Create a new {{language}} project at {{path}} with:
1. Standard directory structure for the language
2. Package/dependency configuration file
3. Main entry point with a hello world example
4. README.md with project name and basic instructions
5. .gitignore appropriate for the language

Use the standard tooling (cargo init, npm init, django-admin startproject, nimble init, etc.)
After creating, verify it builds/runs successfully.
